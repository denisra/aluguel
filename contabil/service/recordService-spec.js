describe('recordService', function() {

  beforeEach(module('contabil'));

  it('should ...', inject(function(recordService) {

	//expect(recordService.doSomething()).toEqual('something');

  }));

});